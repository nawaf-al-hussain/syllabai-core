/**
 * diagnostic module (T-027): explainable, evidence-backed struggle inference.
 *
 * <p>Conservative rule engine over the learner evidence stream: prerequisite-gap
 * (weak prerequisite mastery), exam-literacy 3b (procedural fluency gap — timed
 * vs untimed accuracy), and metacognitive 5a (self-doubt flag). Unsupported
 * struggle categories are never fabricated. Every inference is versioned
 * (rules-v0.2), evidence-backed, probability-bearing, timestamped, expiring
 * (7 days), superseded-but-preserved on re-assessment, and teacher-overridable
 * (Master Spec §17).</p>
 */
package com.syllabai.diagnostic;
